# 🚀 快速上传到 GitHub - 超详细步骤

## 第一步：安装 Git

### Windows 系统
1. 访问 https://git-scm.com/download/win
2. 下载并安装 Git（一直点 Next 默认安装即可）
3. 安装完成后，**重启电脑**或重启命令行
4. 验证安装：打开新的命令行窗口，输入 `git --version`

### Mac 系统
```bash
# 使用 Homebrew 安装（如果没有 Homebrew，先安装）
brew install git

# 或者下载安装包：https://git-scm.com/download/mac
```

---

## 第二步：注册 GitHub

1. 访问 https://github.com
2. 点击右上角 "Sign up" 注册账号
3. 验证邮箱并登录

---

## 第三步：在 GitHub 上创建仓库

1. 登录 GitHub 后，点击右上角的 "+" 号
2. 选择 "New repository"
3. 填写：
   - Repository name: `campus-delivery` (或您喜欢的名字)
   - Description: `校园外卖专送系统` (可选)
   - 选择 Public（公开仓库）
   - **不要**勾选任何初始化选项
4. 点击 "Create repository"
5. **记住**页面上显示的仓库地址，格式是：
   `https://github.com/您的用户名/仓库名.git`

---

## 第四步：上传代码（Windows）

### 方法1：使用 Git Bash（推荐）

1. 在项目文件夹 `E:\SOLOText\2SO` 上，**右键**
2. 选择 "Git Bash Here"
3. 在打开的窗口中，依次运行：

```bash
# 1. 初始化 Git
git init

# 2. 配置您的信息（替换成您的）
git config user.name "您的名字"
git config user.email "您的邮箱@example.com"

# 3. 添加所有文件
git add .

# 4. 提交
git commit -m "Initial commit: 校园外卖专送系统"

# 5. 连接到 GitHub（替换为您的仓库地址）
git remote add origin https://github.com/您的用户名/仓库名.git

# 6. 推送代码
git branch -M main
git push -u origin main
```

### 方法2：使用 GitHub Desktop（最简单！）

1. 下载安装：https://desktop.github.com
2. 打开 GitHub Desktop，登录您的账号
3. 点击 "File" → "Add Local Repository"
4. 选择我们的项目文件夹 `E:\SOLOText\2SO`
5. 点击 "Publish repository" 按钮
6. 填写仓库信息后发布

---

## 第五步：验证

打开浏览器，访问您的 GitHub 仓库地址，应该可以看到所有文件了！

---

## ⚠️ 常见问题

**问题：git 命令找不到？**
- 确保安装了 Git 并重启了命令行
- 可以尝试使用 Git Bash 而不是 CMD/PowerShell

**问题：提示需要登录？**
- 首次推送需要 GitHub 账号
- 如果提示密码，需要使用 Personal Access Token
  - 登录 GitHub → Settings → Developer settings → Personal access tokens
  - 生成新 token，勾选 repo 权限
  - 推送时输入这个 token 作为密码

---

## 下一步

上传成功后，查看 [DEPLOYMENT.md](DEPLOYMENT.md) 进行部署！

---

## 需要帮助？

- Git 官方文档：https://git-scm.com/doc
- GitHub 帮助：https://docs.github.com
- GitHub Desktop 下载：https://desktop.github.com
